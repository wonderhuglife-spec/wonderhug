# WonderHug.Life — environment template
# Copy to web/.env.local. Never commit secrets.
# Service-role keys must never appear in web/ or mobile/.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SITE_URL=https://wonderhug.life
