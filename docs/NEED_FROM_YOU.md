# Still needed from WonderHug (do not invent)

Functional product is complete with labelled slots for these five owner-supplied items:

1. **Official logo and brand photography** — current lockup is `web/public/logo.svg` and editorial SVGs. Drop files into `web/public/` and point `Logo` / hero `<img>` at them.
2. **Verified expert bios, credentials, testimonials** — faculty seats book via WhatsApp. Replace `experts` rows in Supabase / admin. Do not publish invented hospitals.
3. **Counsel-reviewed Privacy Policy and Terms** — `/privacy` and `/terms` are operational notices describing current behaviour until counsel copy arrives.
4. **Razorpay, AiSensy, and Supabase production credentials** — `.env.example` lists names. Demo checkout and `wa.me` work without them.
5. **App Store / Play listing assets** — `/download` explains native run; store badges wait on URLs and screenshots.

Everything else (week notes, programmes, shop, i18n, tools, CMS schema, RLS, CI) is filled and editable.
